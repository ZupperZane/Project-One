rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary debug baseline:
    // any authenticated user can read/write any Firestore document.
    // This removes complex rule expressions while we isolate runtime issues.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
